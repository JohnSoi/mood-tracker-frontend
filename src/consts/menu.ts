/**
 * Время задержки в миллисекундах для debounce обработчика события resize
 * @constant
 * @type {number}
 * @default 300
 *
 * @remarks
 * Используется для оптимизации производительности при обработке событий изменения размера окна.
 * Debounce предотвращает чрезмерное выполнение функции при быстрых последовательных изменениях размера,
 * выполняя её только после завершения серии событий.
 *
 * ⚡ **Оптимизация производительности**:
 * - Событие resize может вызываться десятки раз в секунду при изменении размера окна
 * - Без debounce это приводит к излишней нагрузке на браузер и задержкам интерфейса
 * - Debounce гарантирует, что обработчик выполнится только один раз после прекращения изменений
 *
 * 🎯 **Рекомендуемые значения**:
 * - 100-200ms: Для мгновенного отклика (редкие обновления)
 * - 250-300ms: Баланс между отзывчивостью и производительностью (рекомендуется)
 * - 400-500ms: Для тяжелых вычислений (сложные перерисовки)
 *
 * @example
 * ```typescript
 * // Использование с нативным JavaScript
 * let resizeTimeout: number;
 *
 * window.addEventListener('resize', () => {
 *   clearTimeout(resizeTimeout);
 *   resizeTimeout = setTimeout(() => {
 *     handleResize();
 *   }, DEBOUNCE_RESIZE_HANDLER_MS);
 * });
 *
 * // Использование с Vue composable
 * const handleResize = debounce(() => {
 *   updateLayout();
 * }, DEBOUNCE_RESIZE_HANDLER_MS);
 *
 * // Использование в React hook
 * useEffect(() => {
 *   const handleResize = debounce(() => {
 *     setWindowSize({ width: window.innerWidth, height: window.innerHeight });
 *   }, DEBOUNCE_RESIZE_HANDLER_MS);
 *
 *   window.addEventListener('resize', handleResize);
 *   return () => window.removeEventListener('resize', handleResize);
 * }, []);
 * ```
 *
 * @example
 * ```typescript
 * // Без debounce (плохая практика)
 * window.addEventListener('resize', () => {
 *   // Вызывается 50-100 раз при изменении размера на 100px
 *   expensiveCalculation();
 * });
 *
 * // С debounce (оптимизированная версия)
 * window.addEventListener('resize', debounce(() => {
 *   // Вызывается 1 раз после завершения изменения размера
 *   expensiveCalculation();
 * }, DEBOUNCE_RESIZE_HANDLER_MS));
 * ```
 */
const DEBOUNCE_RESIZE_HANDLER_MS: number = 300;

export { DEBOUNCE_RESIZE_HANDLER_MS };
